/// Bumped whenever the onset-detection or chart-generation algorithm changes,
/// so stale cached charts are regenerated. (Engine lands in M2.)
public enum AnalysisVersion {
    public static let current = 1
}
